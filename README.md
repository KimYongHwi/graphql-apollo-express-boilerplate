# graphql-apollo-express-boilerplate


## main module
> graphql, apollo, express, sequelize, elasticsearch

## playing locally
> docker-compose up -d

## project structure
```
src
├── config
├── domains
├── repositories
│   ├── db
│   │   └── models
│   └── es
└── services
```